{
  "name": "configurator-web",
  "private": true,
  "scripts": {
    "install:configurator": "npm --prefix ./configurator-main install",
    "install:example": "npm --prefix ./example-main install",
    "install:all": "npm run install:configurator && npm run install:example",
    "build:configurator": "npm --prefix ./configurator-main run build",
    "build": "npm --prefix ./example-main run build",
    "preview": "npm --prefix ./example-main run preview",
    "dev": "npm run install:configurator && npm run build:configurator && npm run install:example && npm --prefix ./example-main run dev",
    "dev:nobuild": "npm --prefix ./example-main run dev",
    "build:preview": "npm run build && npm run preview"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.6.0"
  }
}
